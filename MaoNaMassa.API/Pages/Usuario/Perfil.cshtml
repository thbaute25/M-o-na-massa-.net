@page "/usuario/{id:guid}/perfil"
@model MaoNaMassa.API.Pages.Usuario.PerfilModel
@{
    ViewData["Title"] = "Perfil do Usuário";
}

<div class="container py-4">
    <div class="row">
        <div class="col-md-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/Home">Início</a></li>
                    <li class="breadcrumb-item active">Perfil do Usuário</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h2>Perfil do Usuário</h2>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <h5>👤 Usuário ID: @Model.UsuarioId</h5>
                        <p>Use a API para buscar os detalhes do usuário.</p>
                        <p><strong>Endpoint:</strong> <code>GET /api/usuarios/@Model.UsuarioId</code></p>
                        <a href="/swagger#/Usuarios/Usuarios_GetById" target="_blank" class="btn btn-primary btn-sm">Testar no Swagger</a>
                    </div>

                    <div class="mt-3">
                        <h5>Endpoints Relacionados:</h5>
                        <ul>
                            <li><code>GET /api/usuarios/@Model.UsuarioId</code> - Detalhes do usuário</li>
                            <li><code>GET /api/certificados/usuario/@Model.UsuarioId</code> - Certificados do usuário</li>
                            <li><code>GET /api/profissionais?usuarioId=@Model.UsuarioId</code> - Perfil profissional (se existir)</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-4">
        <a href="/Home" class="btn btn-secondary">Voltar</a>
    </div>
</div>

