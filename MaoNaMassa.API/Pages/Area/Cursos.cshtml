@page "/area/{area:required}/cursos"
@model MaoNaMassa.API.Pages.Area.CursosModel
@{
    ViewData["Title"] = $"Cursos de {Model.Area}";
}

<div class="container py-4">
    <div class="row">
        <div class="col-md-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/Home">Início</a></li>
                    <li class="breadcrumb-item"><a href="/Cursos">Cursos</a></li>
                    <li class="breadcrumb-item active">@Model.Area</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h2>Cursos de @Model.Area</h2>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <h5>📚 Área: @Model.Area</h5>
                        <p>Use a API para buscar cursos desta área.</p>
                        <p><strong>Endpoint:</strong> <code>GET /api/cursos/search?area=@Model.Area</code></p>
                        <a href="/swagger#/Cursos/Cursos_Search" target="_blank" class="btn btn-primary btn-sm">Testar no Swagger</a>
                    </div>

                    <div class="mt-3">
                        <h5>Endpoints da API:</h5>
                        <ul>
                            <li><code>GET /api/cursos/search?area=@Model.Area</code> - Buscar cursos por área</li>
                            <li><code>GET /api/cursos?area=@Model.Area</code> - Listar cursos da área (com paginação)</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-4">
        <a href="/Cursos" class="btn btn-secondary">Voltar para Cursos</a>
    </div>
</div>

