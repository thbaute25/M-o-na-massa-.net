@page "/servico/{id:guid}"
@model MaoNaMassa.API.Pages.Servico.DetalhesModel
@{
    ViewData["Title"] = "Detalhes do Serviço";
}

<div class="container py-4">
    <div class="row">
        <div class="col-md-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/Home">Início</a></li>
                    <li class="breadcrumb-item"><a href="/Servicos">Serviços</a></li>
                    <li class="breadcrumb-item active">Detalhes</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h2>Detalhes do Serviço</h2>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <h5>🛠️ Serviço ID: @Model.ServicoId</h5>
                        <p>Use a API para buscar os detalhes completos do serviço.</p>
                        <p><strong>Endpoint:</strong> <code>GET /api/servicos/@Model.ServicoId</code></p>
                        <a href="/swagger#/Servicos/Servicos_GetById" target="_blank" class="btn btn-primary btn-sm">Testar no Swagger</a>
                    </div>

                    <div class="mt-3">
                        <h5>Endpoints Relacionados:</h5>
                        <ul>
                            <li><code>GET /api/servicos/@Model.ServicoId</code> - Detalhes do serviço</li>
                            <li><code>GET /api/avaliacoes/servico/@Model.ServicoId</code> - Avaliações do serviço</li>
                            <li><code>POST /api/avaliacoes?usuarioId={id}</code> - Avaliar serviço</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-4">
        <a href="/Servicos" class="btn btn-secondary">Voltar para Serviços</a>
    </div>
</div>

