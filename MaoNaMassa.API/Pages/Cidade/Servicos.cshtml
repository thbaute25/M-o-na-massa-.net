@page "/cidade/{cidade:required}/servicos"
@model MaoNaMassa.API.Pages.Cidade.ServicosModel
@{
    ViewData["Title"] = $"Serviços em {Model.Cidade}";
}

<div class="container py-4">
    <div class="row">
        <div class="col-md-12">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/Home">Início</a></li>
                    <li class="breadcrumb-item"><a href="/Servicos">Serviços</a></li>
                    <li class="breadcrumb-item active">@Model.Cidade</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h2>Serviços em @Model.Cidade</h2>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <h5>🛠️ Cidade: @Model.Cidade</h5>
                        <p>Use a API para buscar serviços nesta cidade.</p>
                        <p><strong>Endpoint:</strong> <code>GET /api/servicos/search?cidade=@Model.Cidade</code></p>
                        <a href="/swagger#/Servicos/Servicos_Search" target="_blank" class="btn btn-primary btn-sm">Testar no Swagger</a>
                    </div>

                    <div class="mt-3">
                        <h5>Endpoints da API:</h5>
                        <ul>
                            <li><code>GET /api/servicos/search?cidade=@Model.Cidade</code> - Buscar serviços por cidade</li>
                            <li><code>GET /api/servicos/search?cidade=@Model.Cidade&precoMaximo=500</code> - Com filtro de preço</li>
                            <li><code>GET /api/servicos/search?cidade=@Model.Cidade&avaliacaoMinima=4</code> - Com filtro de avaliação</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-4">
        <a href="/Servicos" class="btn btn-secondary">Voltar para Serviços</a>
    </div>
</div>

